# Digital Shop USA

Django e-commerce storefront for computers, components, and technology products.

## Run locally

```bash
python -m venv .venv
.venv\\Scripts\\activate
pip install Django django-jazzmin xhtml2pdf sslcommerz-lib passlib Pillow
python manage.py migrate
python manage.py runserver
```
